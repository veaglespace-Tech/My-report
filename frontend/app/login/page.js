import { LoginScreen } from "@/components/screens/AuthScreens";

export const metadata = {
  title: "Login · Myreport",
};

export default function LoginPage() {
  return <LoginScreen role="ADMIN" />;
}

